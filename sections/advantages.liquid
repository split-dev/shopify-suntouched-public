<section class="advantages {% if section.settings.enable-buttons %}advantages__bg{% endif %}" >
  <div class="container">
    <div class="advantages__wrapper d-flex flex-wrap justify-content-center">
      {% for block in section.blocks %}
        {% if block.settings.img %}
          <div class="advantages__tile-box d-flex justify-content-center">
            <div class="advantages__tile">
              <div class="advantages__tile-hover">
                <img class="advantages__img" loading="lazy" src="{{ block.settings.img | img_url: '90x' }}" alt="{{ block.settings.img.alt }}">
                {% if block.settings.hover-img %}
                  <img class="advantages__hover-img" loading="lazy" src="{{ block.settings.hover-img | img_url: '90x' }}" alt="{{ block.settings.hover-img.alt }}">
                {% else %}
                  <img class="advantages__hover-img" loading="lazy" src="{{ block.settings.img | img_url: '90x' }}" alt="{{ block.settings.img.alt }}">
                {% endif %}
              </div>
              <p class="text-center color-text">{{ block.settings.text }}</p>
            </div>
          </div>
        {% endif %}
      {% endfor %}
      {% if section.settings.link_text != blank %}
        <a class="button__gold advantages__btn text-decoration-none" href="{{ section.settings.link }}">{{ section.settings.link_text }}</a>
      {% endif %}
    </div>
  </div>
</section>

{% schema %}
  {
  "name": "Advantages",
  "settings": [
        {
          "type": "checkbox",
          "id": "enable-buttons",
          "default": false,
          "label": "Background"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        },
        {
          "type": "text",
          "id": "link_text",
          "label": "Link Text"
        }
    ],
    "blocks": [
      {
      "name": "Item",
      "type": "text",
      "limit": 4,
      "settings": [
        {
          "type": "image_picker",
          "id": "img",
          "label": "Image"
        },
        {
          "type": "image_picker",
          "id": "hover-img",
          "label": "Hover Image"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text"
        }
    ]
    }
  ],
  "presets": [
    {
      "name": "Advantages",
      "category": "New"
    }
  ]
  }
{% endschema %}